{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "/bin/sh -c \"set -ex; echo '[boot] migrate'; node scripts/run-migrate.mjs; echo '[boot] srvx'; exec node scripts/serve-production.mjs\"",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
